namespace Alma.Domain.Enum
{
    // enum('pendente','aprovado','recusado','estornado')
    public enum StatusPagamentoDoacao
    {
        Pendente = 1,
        Aprovado = 2,
        Recusado = 3,
        Estornado = 4
    }
}