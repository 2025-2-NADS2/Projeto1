namespace Alma.Domain.Enum
{
    // Mapeia enum('planejado','finalizado','cancelado') do banco.
    public enum StatusEvento
    {
        Planejado = 1,
        Finalizado = 2,
        Cancelado = 3
    }
}